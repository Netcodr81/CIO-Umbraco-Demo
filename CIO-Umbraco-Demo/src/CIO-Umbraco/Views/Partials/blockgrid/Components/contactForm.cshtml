@using CIO_Umbraco_Demo.Views.Partials.Forms.Models
@inherits UmbracoViewPage<BlockGridItem<ContentModels.ContactForm>>

@{
    if (Model.Content is null) return;

    var content = Model.Content;

    var model = new ContactFormViewModel() { FormId = content.FormId!, SuccessMessage = content.SuccessMessage!};
}

@await Html.PartialAsync("~/Views/Partials/Forms/Views/ContactForm.cshtml", model)
