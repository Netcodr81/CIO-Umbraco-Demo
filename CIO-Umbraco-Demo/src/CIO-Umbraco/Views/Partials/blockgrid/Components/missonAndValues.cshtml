@inherits UmbracoViewPage<BlockGridItem<ContentModels.MissonAndValues>>
@using Umbraco.Extensions;

@{
    if (Model?.Content is null) return;

    var content = Model.Content;
}

<div class="mission-and-value__columns-container p-5">
    <div class="grid-container main-content">
        <h2 class="mission-and-value__header">@content.HeaderText</h2>
        <div class="grid g-col-12 g-col-md-6 w-100">
            <p class="g-col-12 g-col-md-6 mission-and-value__subheader">@content.SubheaderText</p>
            <div class="g-col-12 g-col-md-6 mt-0">
                <ul class="mission-and-value__value-list">
                    @if (content?.ValueItems?.Any() == true)
                    {
                        foreach (var item in content.ValueItems)
                        {
                            var listItem = item.Content as IconListItem;
                            <li class="d-flex align-items-center gap-3 mission-and-value__list-item">
                              
                                <inline-svg media-item="listItem?.Icon" fill="@listItem?.IconColor?.Color" width="24" height="24"></inline-svg>
                                @listItem?.Text
                            </li>
                        }

                    }
                </ul>
            </div>
        </div>
    </div>
</div>