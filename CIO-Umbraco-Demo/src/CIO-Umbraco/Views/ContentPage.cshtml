@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<ContentModels.ContentPage>

@{
    Layout = "_Layout.cshtml";
    var content = Model.ContentGrid;

    var pageStyles = Model.PageBackgroundColor?.Color != null ? $"background-color:{Model.PageBackgroundColor?.Color}" : "background-color:#f3f7fc";
}

<!-- START - BLOCK GRID -->
@await Html.GetBlockGridHtmlAsync(Model.ContentGrid)

<!-- END - BLOCK GRID -->